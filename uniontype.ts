//union type
// interface bird { fly(): void;
// layeggs(): void {console.log("bird has laid " + ((Math.random()*10).toFixed).toString + "eggs")};};

// interface fish { swim(): void;
// layeggs(): void {console.log("fish has laid " +((Math.random()*20).toFixed).toString + "eggs ")};};
// there is a bug above to fix

// declare function getsmallpet(): fish | bird;
// let pet = getsmallpet();
// pet.layeggs();
//pet.swim() will output an error since it can only be accessed by one of the two possible types
